{
{% if include_config %}
    "locale": "{{ locale }}",
    "fallback": "{{ fallback }}",
    "defaultDomain": "{{ defaultDomain }}",
{% endif %}
    "translations": {{ translations|json_encode|raw }}
}
